const r=require('express').Router(),db=require('../../services/database');
r.get('/',async(req,res)=>{try{const d=await db.query(`SELECT * FROM competitors WHERE tenant_id=$1 ORDER BY created_at DESC NULLS LAST,id DESC LIMIT 100`,[req.tenantId]);res.json({data:d.rows});}catch(e){res.status(500).json({error:e.message});}});
r.post('/',async(req,res)=>{const b=req.body;try{const d=await db.query(`INSERT INTO competitors(tenant_id,name,website,market_share,revenue_estimate,strengths,weaknesses) VALUES($1,$2,$3,$4,$5,$6,$7) RETURNING *`,[req.tenantId,b.name,b.website,b.market_share,b.revenue_estimate,b.strengths,b.weaknesses]);res.status(201).json(d.rows[0]);}catch(e){res.status(500).json({error:e.message});}});
module.exports=r;