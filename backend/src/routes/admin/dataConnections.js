const r=require('express').Router(),db=require('../../services/database');
r.get('/',async(req,res)=>{try{const d=await db.query(`SELECT * FROM data_connections WHERE tenant_id=$1 ORDER BY created_at DESC NULLS LAST,id DESC LIMIT 100`,[req.tenantId]);res.json({data:d.rows});}catch(e){res.status(500).json({error:e.message});}});
r.post('/',async(req,res)=>{const b=req.body;try{const d=await db.query(`INSERT INTO data_connections(tenant_id,name,provider,connection_type,status,sync_interval) VALUES($1,$2,$3,$4,$5,$6) RETURNING *`,[req.tenantId,b.name,b.provider,b.connection_type,b.status,b.sync_interval]);res.status(201).json(d.rows[0]);}catch(e){res.status(500).json({error:e.message});}});
module.exports=r;