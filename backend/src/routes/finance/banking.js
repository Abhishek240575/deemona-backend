const r=require('express').Router(),db=require('../../services/database');
r.get('/',async(req,res)=>{try{const d=await db.query(`SELECT * FROM bank_accounts WHERE tenant_id=$1 AND is_active=TRUE`,[req.tenantId]);res.json({accounts:d.rows});}catch(e){res.status(500).json({error:e.message});}});
r.get('/:id/transactions',async(req,res)=>{const{from,to,category,limit=100}=req.query;try{const d=await db.query(`SELECT * FROM bank_transactions WHERE bank_account_id=$1 AND tenant_id=$2 ORDER BY transaction_date DESC LIMIT ${limit}`,[req.params.id,req.tenantId]);res.json({transactions:d.rows});}catch(e){res.status(500).json({error:e.message});}});
r.post('/:id/transactions',async(req,res)=>{const b=req.body;try{const d=await db.query(`INSERT INTO bank_transactions(tenant_id,bank_account_id,transaction_date,amount,type,description,reference,category) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *`,[req.tenantId,req.params.id,b.transaction_date,b.amount,b.type,b.description,b.reference,b.category]);await db.query(`UPDATE bank_accounts SET current_balance=current_balance+$1 WHERE id=$2`,[b.amount,req.params.id]);res.status(201).json(d.rows[0]);}catch(e){res.status(500).json({error:e.message});}});
module.exports=r;