const r=require('express').Router(),db=require('../../services/database');
r.get('/',async(req,res)=>{try{const d=await db.query(`SELECT * FROM energy_usage WHERE tenant_id=$1 ORDER BY created_at DESC NULLS LAST,id DESC LIMIT 100`,[req.tenantId]);res.json({data:d.rows});}catch(e){res.status(500).json({error:e.message});}});
r.post('/',async(req,res)=>{const b=req.body;try{const d=await db.query(`INSERT INTO energy_usage(tenant_id,facility,source_type,is_renewable,amount_kwh,cost,period_date,target_kwh) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *`,[req.tenantId,b.facility,b.source_type,b.is_renewable,b.amount_kwh,b.cost,b.period_date,b.target_kwh]);res.status(201).json(d.rows[0]);}catch(e){res.status(500).json({error:e.message});}});
module.exports=r;