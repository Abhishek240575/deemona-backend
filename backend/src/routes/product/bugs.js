const r=require('express').Router(),db=require('../../services/database');
r.get('/',async(req,res)=>{try{const d=await db.query(`SELECT * FROM bugs WHERE tenant_id=$1 ORDER BY created_at DESC NULLS LAST,id DESC LIMIT 100`,[req.tenantId]);res.json({data:d.rows});}catch(e){res.status(500).json({error:e.message});}});
r.post('/',async(req,res)=>{const b=req.body;try{const d=await db.query(`INSERT INTO bugs(tenant_id,title,severity,priority,status,assigned_to,feature_id,reported_by) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *`,[req.tenantId,b.title,b.severity,b.priority,b.status,b.assigned_to,b.feature_id,b.reported_by]);res.status(201).json(d.rows[0]);}catch(e){res.status(500).json({error:e.message});}});
module.exports=r;